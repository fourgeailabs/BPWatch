# Gradle / Android build outputs
build/
.gradle/
local.properties
*.iml
.idea/
# Signing keys (never commit)
*.jks
*.keystore
# OS noise
.DS_Store
.kotlin/
# Exception: the pinned *debug* keystore is intentionally committed so every
# build (local + CI) shares one signature and updates preserve user data.
# Release keys must still never be committed.
!keystore/bpwatch-debug.keystore
